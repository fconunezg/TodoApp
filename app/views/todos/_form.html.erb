<%= form_with(model: @todo, local: true) do |f| %>
  <%= f.label :description, "Descripcion ToDo" %>
  <%= f.text_field :description %>

  <%= f.label :completed, "Completed?" %>
  <%= f.text_field :completed %>

  <%= f.submit "Enviar!" %>
<% end  %>
